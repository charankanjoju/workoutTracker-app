import React from 'react';
import './WorkoutContent.css'; // Create a CSS file for custom styling if needed

const WorkoutContent = () => {
    return (
        <div className="workout-content">
           
            
        </div>
    );
};

export default WorkoutContent;
